// Image class
class Image {
    constructor(
        // Defines parameters:
        src,
        alt,
        id,
    ) {
        // Defines Properties
        this.src = src;
        this.alt = alt;
        this.id = id;
    }
}

export default Image;